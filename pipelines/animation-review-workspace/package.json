{
  "name": "@gameai/animation-review-workspace",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @gameai/animation-review-ui build && pnpm run build && tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js",
    "start": "pnpm --filter @gameai/animation-review-ui build && pnpm run build && node dist/cli.js"
  },
  "dependencies": {
    "@gameai/animation-review-core": "workspace:*",
    "@gameai/animation-review-ui": "workspace:*",
    "@gameai/character-contracts": "workspace:*",
    "@gameai/rig-animation": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
