# TASK-014B Cocos Semantic VFX Adapter Acceptance

## Result

Implementation acceptance gate: **PASS**.
External visual review: **PASS**.

TASK-014B is the first visual Cocos Creator 3.8.8 consumer of Character
Semantic Events 1.0. Engine-neutral evaluator commands drive three procedural
Cocos-only renderers at real runtime sockets: one-shot footstep dust, a
looping hand trail, and one coalesced persistent torso aura. Audio and gameplay
commands remain diagnostic counts only.

## Transform Stress control

- Registry action: `toggle-transform-stress`
- Action ID: `transform-stress.toggle`
- Display key / Cocos key: `X` / `KEY_X`
- HUD label: `Transform Stress`
- Initial and Exact Reset state: `STRESS OFF`
- Stress root: one `TransformStressRoot`, created once outside the unchanged
  authored character/socket hierarchy
- Stress contribution: translation `(84, -48)`, rotation `17deg`, scale
  `(1.18, 0.82)`
- Authored inner baseline: translation `(100, 60)`, scale `(1.35, 1.35)`,
  baseline rotation unchanged

The outer root is never reparented during toggling. Rebuild recreates one
runtime tree, preserves and reapplies the current Stress state, and retains
one input handler. Exact Reset restores both the runtime state and physical
root transform to Stress OFF.

## Automated verification

- Extension TypeScript: PASS.
- Cocos clean-CI typecheck: PASS.
- Focused TASK-014B tests: 22/22 PASS.
- Complete extension tests: 216/216 PASS.
- Semantic-event package tests: 24/24 PASS.
- Working-copy `CI=true pnpm verify`: 373/373 PASS.
- Frozen tracked-files-only install and `CI=true pnpm verify`: 373/373 PASS.
- Generated source/runtime mirror closure: PASS.
- Schema byte identity: PASS.
- Metadata-race regression: 3/3 PASS.
- `git diff --check`: PASS.
- Tracked MP4 count before feature publication: 0.

Structured tests cover unique registry ownership, the typed action union,
HUD/handler parity, deterministic ON/OFF state, finite nested matrix
composition, translation/non-uniform-scale/rotation stress, unknown-action
fail-closed behavior, reset and rebuild semantics, single-root/single-input
ownership, source/generated byte identity, shared transformed socket poses
for all three renderers, persistent coalescing, cleanup, and absence of a
Cocos Transform Stress control from the engine-neutral package.

## Creator 3.8.8 gate

- Cold import/open and component identity: PASS.
- R1 → TASK-014B → R1 → TASK-014B: PASS.
- Missing class: 0.
- Invalid component: 0.
- Initial resources/HUD/Rest: 1/1 PASS, Rest, `STOPPED`, `0.00s`.
- Walk dust and cleanup: PASS.
- Wave trail start/follow/stop: PASS.
- Persistent aura six-loop single-instance behavior: PASS.
- Pause/Resume with the same aura instance: PASS.
- Track switch/stop cleanup: PASS.
- Stress OFF → ON → OFF: PASS.
- Walk, Wave, and six-loop Aura under Stress ON: PASS.
- Two lifecycle rebuilds under Stress ON: PASS; final lifecycle before reset
  `SETUP 3 / TEARDOWN 2 / REBUILDS 2 / INPUT 1`.
- Walk, Wave, and six-loop Aura after rebuild: PASS.
- Exact Reset and two-second final hold: PASS.
- Creator Console: 0 relevant warnings/errors.
- Web Preview Console: 0 warnings/errors.

## Runtime measurements

- Stress root position: `(84, -48)`
- Stress root rotation: `17deg`
- Stress root scale: `(1.18, 0.82)`
- Dust socket position/rotation error: `0.0000px` / `0.0000deg`
- Trail socket position/rotation error: `0.0000px` / `0.0000deg`
- Aura socket position/rotation error: `0.0000px` / `0.0000deg`
- Projection round-trip error: `0.0000px`
- Persistent aura visible/evaluator/adapter/UIRenderer/Sorting2D count:
  `1/1/1/1/1`
- Persistent aura instance ID:
  `aura-semantic-events:torso-persistent-aura:0`
- Duplicate starts / unknown stops / leaked instances: `0 / 0 / 0`
- Renderer conflicts / duplicate roots / duplicate inputs / stale nodes:
  `0 / 0 / 0 / 0`
- NaN/Infinity values: 0

Aura scale and follow behavior visibly consume the stressed torso world
transform. Dust and trail likewise follow their stressed real socket world
poses; no Canvas compensation is present.

## Final state and protected content

Exact Reset ends at authored Rest, `STOPPED`, `0.00s`, Stress OFF, baseline
transform, zero active VFX, Debug OFF, and zero conflict/duplicate/leak/stale
diagnostics.

Creator did not rewrite the tracked Scene or `.meta`:

- Scene SHA-256:
  `abea453c7cfa2648c18944d56e42dd43f298cc5c49c59898c267856eb14369c3`
- Scene `.meta` SHA-256:
  `fb150603bfb20a5f2645ef0fb56231e41dd6662e0b73bf313d83743c9d1ecd52`

No TASK-014C work, production audio/gameplay execution, feature-branch media,
or Cocos Transform Stress control in the engine-neutral semantic-events
package is included.

## External visual acceptance

- Review result: **PASS**.
- Reviewed feature SHA:
  `ebcb087426b0e2519f437e9706658d59f9da18a7`.
- Reviewed evidence SHA:
  `9e326f68337c61156a4be19576965e0b5f7e65ae`.
- Reviewed replacement video SHA-256:
  `5c1eeb1c574549ae4e7aafd9fd0b2bdfb5f882772a99d4404f75c6a2d4c54a4a`.
- Dust is clearly visible.
- The green/white double-layer Trail curve is clearly visible.
- Aura is complete and remains a single instance across six loops.
- Normal and Stress viewport: PASS.
- Complete HUD: PASS.
- Rebuild: PASS.
- Exact Reset: PASS.
- The replacement recording's final cumulative lifecycle is
  `SETUP 7 / TEARDOWN 6 / REBUILDS 6 / INPUT 1`. The cumulative values
  include multiple rebuilds during recording; each stable state remains at
  one root, one input handler, and zero leaks.
- A small number of macOS recording pointer/pointer-trail artifacts are
  present. They do not obscure Dust, Trail, or Aura and do not affect
  functional acceptance. This report does not claim
  `pointerOutsideCanvas: true` for the full replacement recording.

The evidence manifest is classified `passed-external-visual-review`. The
temporary evidence branch may be removed only after the acceptance
documentation is pushed, the Draft PR exists, and PR CI passes.
